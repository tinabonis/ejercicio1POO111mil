
package ejercicios;

public class OperacionesArreglos{
    /**
     * Quita TODAS las ocurrencias de un elemento dentro de un arreglo.
     * @param arreglo un arreglo de enteros.
     * @param elemento un elemento entero.
     * @return devuelve un arreglo con un tamanio reducido si se realizo la 
     * remocion y SIN el elemento especificado. No es necesario
     * conservar el orden original de los elementos.
     * En el caso que se hayan removido TODOS los elementos del arreglo debe
     * retornarse null.
     */
	public static int[] removerElemento(int[] arreglo, int elemento){
            return null;
	}
}