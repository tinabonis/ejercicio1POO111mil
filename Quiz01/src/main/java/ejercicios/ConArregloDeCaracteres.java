
package ejercicios;

/**
 *
 * @author steve-urbit
 */
public class ConArregloDeCaracteres {
     /**
     * Calcula la cantidad de palabras dentro de un arreglo de caracteres.
     * El arreglo NO contiene espacios al inicio ni al final
     * las palabras estan separadas SOLO por un espacio.
     * @param arreglo
     * @return cantidad de palabras en el arreglo.
     */
    public static int cantidadPalabras(char[] arreglo){
        return -1;
    }
    
    /**
     * Valida si el password cumple con las condiciones:
     * 1. Longitud minima de 8 caracteres.
     * 2. Solo puede contener letras minusculas y digitos.
     * 3. Debe contener por lo menos dos digitos.
     * Hint: mirar la tabla UTF-8(ASCII) ver codigos equivalentes para los
     * valores permitidos.
     * @param password
     * @return 
     */
    public static boolean validarPassword(char[] password){
        return false;
    }
}
